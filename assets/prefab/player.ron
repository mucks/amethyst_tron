#![enable(implicit_some)]

Prefab (
    entities: [
        (
            data: (
                graphics: (
                    mesh: Asset(File("mesh/player.obj", ("OBJ", ()))),
                    material: (
                        albedo: Generate(Srgba(0.0, 0.0, 1.0, 1.0))
                    )
                ),
                transform: (
                    translation: (0.0, 0.0, -4.0)
                )
            )
        ),
    ]
)